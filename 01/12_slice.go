// package main

// import "fmt"

// func main() {
// 	// var firstSlice []int = []int{1, 2, 3, 4, 5}
// 	// var secondSlice []int = firstSlice

// 	// secondSlice = append(secondSlice, 8)
// 	// secondSlice[0] = 1000

// 	// fmt.Println(firstSlice, cap(firstSlice), len(firstSlice))
// 	// fmt.Println(secondSlice, cap(secondSlice), len(secondSlice))

// 	sampleArray := [6]int{1, 2, 3, 4, 5, 6}
// 	sampleSlice := sampleArray[1:3]
// 	sampleSlice = append(sampleSlice, 0, 0, 0, 0, 0, 0, 0)
// 	sampleArray[1] = 55

// 	fmt.Println(sampleSlice, cap(sampleSlice), len(sampleSlice))
// 	fmt.Println(sampleArray, cap(sampleArray), len(sampleArray))

// }
